+++
date = '2026-05-14T08:19:38+08:00'
draft = false
title = '实用指令'
+++

### 1. 查端口占用 PID
1:把端口号换成你要查的，比如 8080、3306
```cmd
netstat -ano | findstr 端口号
```
输出最后一列就是 PID

2. 通过 PID 查是哪个程序
```cmd
tasklist | findstr PID号
```
3. 强制关闭占用端口的进程
```cmd
taskkill /F /PID PID号
```